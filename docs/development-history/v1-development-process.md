# 早期 V1 开发流程摘要

早期版本采用“先建立可运行闭环，再逐步替换 Mock 能力”的开发顺序：

```text
项目骨架
→ 本地数据模型
→ 群聊读取接口
→ 确定性排行榜
→ AI 摘要与 Prompt
→ 本地归档
→ 邮件与调度
→ 页面和测试
```

每个阶段都要求保留无真实外部副作用的测试路径。真实微信、模型和邮件只作为单独环境验收，不用自动化测试结果代替。

原始流程文档中的真实名称、消息样例、精确统计和本机路径不适合公开，当前文件仅保留可复用的方法摘要。
